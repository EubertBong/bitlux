# Base URL of the API (no trailing slash). Same-site with the SPA in local dev
# (localhost:5173 -> localhost:8000), so the HttpOnly refresh cookie flows.
VITE_API_BASE_URL=http://localhost:8000/api/v1
